[metadata]
name = nyc-taxi-clickhouse-etl
version = 0.1.0

[options]
packages = find:
python_requires = >=3.11

[options.packages.find]
exclude =
    mutants*

[tool:pytest]
testpaths = extract/tests/
addopts = --ignore=extract/tests/test_fuzz.py
